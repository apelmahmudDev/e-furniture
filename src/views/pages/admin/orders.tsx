import { Box } from "@mui/material";
import OrdersTable from "../../components/admin/Orders";

const Orders = () => {
	return (
		<Box mb={2}>
			<OrdersTable />
		</Box>
	);
};

export default Orders;
